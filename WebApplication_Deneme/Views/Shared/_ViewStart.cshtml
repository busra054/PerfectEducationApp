@{
    Layout = User.Identity.IsAuthenticated
        ? (User.IsInRole("Öğrenci") ? "_StudentLayout" :
           User.IsInRole("Öğretmen") ? "_TeacherLayout" :
           User.IsInRole("Admin") ? "_AdminLayout" : "_Layout")
        : "_Layout";
}

